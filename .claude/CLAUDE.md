# 基本

- 共通ルールは以下を参照。
@~/.codex/AGENTS.md

## このリポジトリの前提

- WSL と Windows Terminal の併用を前提にしている。
- `bin/initialize` 配下のスクリプトは symlink ベースのセットアップを壊さない。
- shell script は Bash 前提で、可読性を保つ。
- `.env` や秘密情報を前提にした提案は避ける。
- Windows 側のパスやユーザー名を必要以上に固定しない。
